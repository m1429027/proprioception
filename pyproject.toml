[project]
name = "shoulder-laser-rom"
version = "0.1.0"
description = "Shoulder laser projection measurement system"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
  "numpy>=1.21,<2",
  "opencv-contrib-python>=4.6,<5",
]

[tool.uv]
package = false
